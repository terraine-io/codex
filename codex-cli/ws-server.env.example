# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Anthropic API Configuration (if using Claude)
# ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Google API Configuration (if using Gemini)
# GOOGLE_API_KEY=your-google-api-key-here

# Working Directory (optional)
# WORKING_DIRECTORY=/path/to/your/project

# Model Configuration
# MODEL=gpt-4
# PROVIDER=openai
# INSTRUCTIONS=You are a helpful coding assistant.

# Context Management Configuration
# Context management strategy: threshold (default) or dummy
# - threshold: Auto-compacts when usage exceeds threshold
# - dummy: Never auto-compacts (manual compaction only)
CONTEXT_STRATEGY=threshold

# Context compaction threshold (0.0 to 1.0, default: 0.8)
# 0.8 means compact when context reaches 80% of the model's limit
# Only used with threshold strategy
CONTEXT_COMPACTION_THRESHOLD=0.8

# WebSocket Server Configuration
# WS_PORT=8080

# CORS Configuration for WebSocket connections
# Comma-separated list of allowed origins for browser-based clients
# If not set or empty, defaults to http://localhost:8081 for security
# Example: ALLOWED_ORIGINS=http://localhost:3000,https://myapp.com
# ALLOWED_ORIGINS=http://localhost:3000,https://myapp.com